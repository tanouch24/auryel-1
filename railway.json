{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn auryel_bot:app --bind 0.0.0.0:$PORT --worker-class gthread --workers 1 --threads 8 --timeout 120"
  }
}
