{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn auryel_bot:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
  }
}
